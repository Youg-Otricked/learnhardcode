using System;

namespace Chronoshard.core
{
    class Program
    {
        public static int Main(string[] args)
        {
            string PlayerName = "Name";
            string GameName = "ChronoShard";

            Console.WriteLine($"Player {PlayerName} has logged into {GameName}.");
            return 0;
        }
    }
}